# This is here for later if I wish to expand on the site.
# The models many consist of users, so that members can have films recomended to them and saved in the collections page.
# This would require the films in the users current collection can not be recomended again so need filtering out also.